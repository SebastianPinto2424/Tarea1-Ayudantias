
/*5. Crea un array de 3 números, multiplícalos por 2 y guárdalos en un nuevo array usando map.*/

const arregloNumeros = [20, 40, 80];
const arregloMultiplicado = arregloNumeros.map(numero => numero * 2);

console.log(arregloMultiplicado);

